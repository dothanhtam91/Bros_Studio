/**
 * Optional on-site playback via YouTube embed (user must tap to start; browsers block silent autoplay).
 * Ensure you have rights to use this track on your commercial site (licensing / YouTube terms).
 */
export const SITE_AMBIENT_MUSIC = {
  youtubeVideoId: "emJtURJmVmk",
} as const;
